/src
  main.py                # entry point simulazione Arcade

  world/
    world.py             # mappa, campi climatici e biomi
    biome.py             # definizione biomi e colori
    map_generator.py     # generazione procedurale del mondo
    config.py            # costanti globali del clima
    environment.py       # canali ambientali per bioma (LIGHT, H2S, ecc.)
    phisics.py           # movimento / interazioni base (placeholder)
    resources.py         # atomi, risorse e ResourceGrid

  pixel/
    pixel.py             # singolo pixel (cellula) per debug/serializzazione
    genome.py            # rappresentazione e similarità genomica
    metabolism.py        # risorse interne, metabolismo e costi
    reproduction.py      # logica di divisione 1->2 e helper sessuali
    behaviors.py         # AI elementare (heuristiche di movimento)
    pixel_manager.py     # gestione di tutti i pixel, update per tick
    traits.py            # tratti evolutivi di alto livello e prerequisiti

  commands/
    mouse_input.py       # gestione input mouse e delega ad ArcadeRenderer

  rendering/
    renderer.py          # interfaccia astratta dei renderer
    arcade_renderer.py   # renderer grafico principale (Arcade + GUI)
    debug_renderer.py    # renderer testuale/headless

  ui/
    input.py             # helper input e bottoni UI
    panels.py            # layout e pannelli stile macOS

